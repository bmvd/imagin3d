List of required PIP installs is still in developement. 

To run this Python Code meant for Image to 3D-Model generation, an API token from Replicate is necessairy. Furthermore, within the SketchToModel.py file, filepaths need to be tuned to your system. 

After setting all these settings, run the imagin3d.py script through the Windows Command Prompt to start the process. 